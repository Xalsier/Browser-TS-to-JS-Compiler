// Type Annotation
let variable = 5;

// Decorator
function decoratorExample(target, propertyKey, descriptor) {
    console.log("Method was called");
}

class ExampleClass {
    // Access Modifier
    privateVariable = "Test";

    // Generic Type
    genericMethod(arg) {
        return arg;
    }

        method() {
        console.log("Method executed");
    }
}

// Interface Declaration

// Enum
const Color = {
  Red: "RED",
  Green: "GREEN",
  Blue: "BLUE"
};